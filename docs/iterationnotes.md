* structure the output from the dramaturg in the chat
* for single script, upload pdf in projects page, not just within the project
* when adding a script within a project, a modal should pop up asking what episode it is etc, similiar to the modal when you create a new multi-script project in the projects interface
* be able to add multiple images via Add to canvas button
